# Project 6 

This time, it is a simple To Do console that let you create task with a set date, the task manager of the console program checks for over due tasks, tasks that are pending or finished, though the list are temporary as a template holder because it will clear and make a new list

V1: basic functions mentioned above

V2 ( TBA ): version 2 will use UUID of the task to optimize accessing/manage the task behind the background of the program
